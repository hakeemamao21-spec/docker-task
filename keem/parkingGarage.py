#vehicle_size = input("Enter the size of your vehicle (small, medium, large): ")

#if vehicle_size == "small":
    #vehicle_num = float(input("Enter the number of vehicles") or 1)
    #total = vehicle_num * 100
    #print(f" you will need to pay: ${total}")
#elif vehicle_size == "medium":
    #vehicle_num = float(input("Enter the number of vehicles")or 1)
    #total = vehicle_num * 150
    #print(f" you will need to pay: ${total}")
#elif vehicle_size == "large":
    #vehicle_num = float(input("Enter the number of vehicles")or 1)
    #total = vehicle_num * 200
    #print(f" you will need to pay: ${total}")
#xelse:
    #print("Enter a correct size")


#def testEven(myinput):
  #if(myinput%2==0):
    #print("the given number is even")
  #else:
   # print("the given number is odd")

#def Area(length ,width):
    #return width*length

#print(Area(5,21))

#def f(x):
   # x = x+1
    #return x

#x =6
#print(f(x))
#print(x)

#today = ("silas" , 23 , "damian", 56, "tarea"

tswift = ((2014, "katy"),
          (2014, "harry"),
          (2012, "jake"),
          (2010, "taylor"),
          (2003, "Beyonce"),
          (2025, "Damiain"),
          (2008, "Joe"))

def FindPerson(t):
    earliest = 2025
    name = ""
    for p in t:
        if (p[0]<earliest):
            print()

             